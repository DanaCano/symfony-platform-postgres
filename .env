APP_ENV=dev
APP_DEBUG=1
APP_SECRET=change_this_secret_in_prod

# PostgreSQL por defecto (para cumplir con el stack del puesto)
DATABASE_URL="postgresql://app:app@127.0.0.1:5432/app_db?serverVersion=16&charset=utf8"

# SQLite opcional para pruebas locales rápidas:
# DATABASE_URL="sqlite:///%kernel.project_dir%/var/data.db"
