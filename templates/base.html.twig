<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{% block title %}Plataforma Symfony{% endblock %}</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="{{ asset('css/style.css') }}">
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-light bg-light fixed-top">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('app_home') }}">🌐 Plataforma</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
      <ul class="navbar-nav">
        {% if app.user %}
          <li class="nav-item"><a class="nav-link" href="{{ path('app_home') }}">Proyectos</a></li>
          <li class="nav-item"><a class="nav-link" href="{{ path('app_project_new') }}">Nuevo Proyecto</a></li>
          <li class="nav-item"><a class="nav-link" href="{{ path('app_project_my_projects') }}">Mis Proyectos</a></li>
          <li class="nav-item"><a class="nav-link" href="{{ path('app_logout') }}">Salir ({{ app.user.name }})</a></li>
        {% else %}
          <li class="nav-item"><a class="nav-link" href="{{ path('app_login') }}">Iniciar Sesión</a></li>
          <li class="nav-item"><a class="nav-link" href="{{ path('app_register') }}">Registro</a></li>
        {% endif %}
      </ul>
    </div>
  </div>
</nav>

<div class="container pt-5">
  {% for message in app.flashes('success') %}
    <div class="alert alert-success">{{ message }}</div>
  {% endfor %}
  {% for message in app.flashes('error') %}
    <div class="alert alert-danger">{{ message }}</div>
  {% endfor %}
  {% block body %}{% endblock %}
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<script src="{{ asset('js/app.js') }}"></script>
</body>
</html>
